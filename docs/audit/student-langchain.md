# Audit group — student-langchain

<!--
  Generated by `npm run audit:langchain`. Do not edit by hand: re-running the
  harness overwrites this file.

  Findings for group `student-langchain`: the student domain, audited by the five
  independently-driven LangChain agents and compared finding-by-finding against
  `student-native.md`. An empty findings table here means "the run reported
  no findings", never "not run" — the group `status` says which.

  The schema, the categories and the escaping rules are in docs/audit/README.md.
  Table columns are fixed and the aggregator fails loudly on a deviation, so a
  reordered column is a hard error rather than a silently dropped row.
-->

## Group

| field          | value             |
| -------------- | ----------------- |
| group          | student-langchain |
| domain         | student           |
| kind           | langchain         |
| status         | reported          |
| agents         | 5                 |
| agentsReported | 5                 |

## Findings

| id   | agent                 | severity | category      | title                                                                                                                                                                                       | evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | location                                                                           | status | dupOf |
| ---- | --------------------- | -------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------ | ----- |
| SL-1 | student-quizzes       | blocker  | broken-flow   | Mid-attempt answers do not survive a refresh (no save path; client resets all answers on reopen)                                                                                            | Getting /api/student/quiz-attempts as demo.student1@school.edu returned the demo quiz as canStart with 4 questions; listing app/api/student/quiz-attempts shows only route.ts (list/start), [attemptId]/route.ts (GET), [attemptId]/submit/route.ts and practice/route.ts — there is no save/autosave endpoint, and the rendered attempt UI (components/student-quiz-attempts.tsx) sets every answer to null in startAttempt() and openAttempt() ('setAnswers(Object.fromEntries(body.attempt.questions.map((q) => [q.id, null])))') and clears textAnswers to {}. Why it matters: A student who answers three of four questions and refreshes, or reopens the in-progress attempt, sees an entirely blank quiz and loses all typed/selections with no warning, because nothing is ever written until Submit. Fixed 2026-09-18: added PUT /api/student/quiz-attempts/[attemptId]/draft backed by saveQuizAttemptDraft; a draft lives in the same QuizResponse rows the submitted attempt uses, getStudentAttempt returns draftAnswers while in progress, the client autosaves (debounced, flushed on pagehide) and hydrates on start/reopen instead of resetting every answer to null, and submit replaces the drafts with scored rows in one transaction. Verified as demo.student1: PUT returned {"success":true,...} and a subsequent GET returned draftAnswers [{questionId..., selectedIndex: 2}] (previously null/absent); a fresh attempt returns []. Covered by new tests in tests/quiz-attempts-pipeline.test.ts. | components/student-quiz-attempts.tsx:121,139 / app/api/student/quiz-attempts/      | fixed  |       |
| SL-2 | student-collaboration | minor    | inconsistency | "Round progress ... Evaluations submitted 3 of 3" counts the student's own ratings while the results gate counts teammates' ratings, so the round looks complete and still shows no results | GET /student/peer-evaluation as demo.student1@school.edu returned 200 and rendered "Round progress — Team Alpha" / "DEMO-MATH-101 · Algebra Foundations" with the progress line "Evaluations submitted 3 of 3"; the same session's GET /api/student/peer-evaluation returns submittedEvaluations:3, expectedEvaluations:3 and received:{withheld:true, ratingCount:2, minRatersRequired:3}, which the component renders as the withheld card "Ratings received 2 of 3 needed", directly under the copy "Results appear only after at least 3 teammates have submitted". Why it matters: A student sees the round reported as 3 of 3 submitted yet is told results are not available because only 2 of 3 ratings are in; the two counters share the same denominator but measure different things (my submissions including my self-rating vs ratings received from others), so the page reads as if it contradicts itself. Caveat (re-verification 2026-09-17): the 'Evaluations submitted 3 of 3' line depended on a self-evaluation the audit created. The seed writes no self-evaluation (3 students x 2 non-self peers; GET now reports submittedEvaluations 2 and selfEvaluationSubmitted false), so /student/peer-evaluation renders '2 of 3' and the 'round complete yet no results' contradiction is no longer visible. The withheld card still reproduces: received withheld true, ratingCount 2, minRatersRequired 3 ('2 of 3 needed'), because a 3-member team can never have 3 non-self raters.                | /student/peer-evaluation (components/student-peer-evaluation.tsx:315-333, 427-436) | open   |       |
| SL-3 | student-quizzes       | minor    | inconsistency | Attempt panel says 'answer all questions, then submit' but Submit quiz silently accepts and scores unanswered questions                                                                     | The rendered in-progress attempt SectionCard description reads 'In progress — answer all questions, then submit.' while the only control is a plain 'Submit quiz' button with no remaining-answer count, no disabled state and no confirmation; the submit payload sends selectedIndex: null for any question the student skipped, and lib/quiz-attempts/service.ts:830-839 persists an unanswered question with isCorrect: null rather than rejecting it. Why it matters: The copy implies submission requires a complete set of answers, but the button fires immediately with blanks that score zero, so a student can submit a half-answered quiz believing the instruction has been enforced.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | components/student-quiz-attempts.tsx:330-331, 421-430                              | open   |       |
